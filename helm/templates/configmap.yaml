apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "aiops-quality.fullname" . }}-env
  labels:
    {{- include "aiops-quality.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
